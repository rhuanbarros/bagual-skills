---
topic: bagual-bmad-loop-runner skill
updated: 2026-08-05T00:56
---

- (decision) Skill name fixed: bagual-bmad-loop-runner. Separate skill that invokes bmad-loop run and supervises from above; explicitly NOT a bmad-loop plugin (owner decision 2026-08-04)
- (decision) bagual-worktree retired in favour of bmad-loop native [scm] isolation=worktree (owner confirmed 2026-08-04)
- (decision) QA gate is optional per project, driven by project config - not hardcoded in the skill
- (decision) All skills get an init mode: on run, check a project config file; if never configured, enter init before working
- (decision) Skill and every artifact it produces are written in English. Existing Portuguese wiki stays as-is; new knowledge from this skill onward is English
- (event) Motivation is not token cost alone: epic-runner is weak in parts (context compaction). Goal is a better orchestration, cheaper as a side effect
- (decision) Audience: owner only, multi-project + multi-org. Several near-identical projects on this machine share one usage shape; a separate company project has its own ticket rules, workflows and MCPs - none of it may be hardcoded in the skill
- (decision) Per-project config lives in a .bagual/ folder at project root: skill configuration, org-specific rules, workflow variations, MCP choices
- (event) Owner wants the knowledge loop reviewed and tightened: where agent-discovered patterns/anti-patterns are stored, and how they are guaranteed to reach epic/story specs at creation time. Rationale: bmad-loop executes literally, so planning must be complete before the night starts
- (decision) Company/org rules live in the project wiki, not in .bagual/. The wiki belongs to the project, so the org rules that govern that project belong there too. .bagual/ holds configuration only
- (decision) Adopt BMad's customize.toml architecture for bagual-* skills: base defaults in the skill, team overrides + user overrides merged per project. Same merge semantics, not a new invention
- (decision) Wiki grounding unified into a single source of truth; bagual-spec-gate owns the rule and every other consumer points at it instead of restating it
- (decision) Working mode: Fast path (owner is on mobile, wants short scannable output)
- (decision) All stories are authored BEFORE the run, and bagual-spec-gate audits the stories themselves - not only the epic doc. Rationale: the story is the contract bmad-loop actually executes, and today it is authored overnight with nobody present to ask
- (decision) Adopt bmad-loop [stories] source=stories mode (stories.yaml + SPEC.md in a spec folder, run via --spec). Already supported natively; currently disabled
- (decision) Story staleness accepted: if reality shifts mid-epic, whoever adjusts a story spec starts from a mostly-complete artifact. Mitigation is a fast re-run of the gate, not upfront defensive design
- (event) PRD drafted (fast path): 25 FRs in 5 groups - init/config, planning gate, run supervision, epic closure, knowledge loop. 7 open questions, 4 assumptions
- (decision) bagual-bmad-loop-runner stays resident but IDLE: it waits on a hook/event and only burns tokens when it must act. Owner runs it in a dedicated separate window for full control
- (decision) Stories are authored by John/the usual BMad story skill, not by a new mechanism. Gate re-run scope: affected story only, but the system must judge that itself rather than follow a fixed rule
- (decision) Confirmed: the loop-runner skill owns running the retrospective (bmad-loop retrospective=auto is unsupported in v1)
- (decision) Distribution: all v2 skills live in the bagual-skills repo under a new v2/ folder, reusing existing skill names. Install ideal = copy the folder into place, run init, works. An install script is optional convenience, not the primary path
- (decision) bagual-gerente-geral is renamed bagual-manager in v2. All skill names in English
- (decision) bagual-manager is the central skill: it does everything in the project for the user, autonomously. bagual-bmad-loop-runner is the ONLY skill the user drives directly, in its own window
- (decision) bagual-manager communication contract: objective, TLDR-first, lists, scannable, short by default, NO abbreviations or internal codes (the user must never have to remember them). Expands only when the user asks for detail
- (decision) bagual-manager must self-learn: when the user corrects it ('in this project we do X', 'we commit .env here', 'we do not use Y'), it writes that lesson into the correct skill config file itself, so the user never re-teaches the same thing
- (decision) bagual-manager must know the skill map and their config files well enough to route a learning to the right place. Example: at company X, opening a ticket via bagual-tickets must first query the ClickUp MCP to pull data and populate the local ticket - the manager knows this belongs in that skill's config
- (decision) Inherited debt (54 mis-triaged tickets, dormant Semgrep/Cerco) belongs to the gorioapp project, not to the skills - but must be dispositioned before the skills work is called finished
- (change) PRD scope widened from one skill to the Bagual Skills v2 suite (two anchor skills: bagual-manager + bagual-bmad-loop-runner). Shared config/naming/distribution/knowledge layers made splitting it into two drifting documents the worse option
- (event) FR26-FR33 added (manager role, communication contract, self-learning, distribution). 5 of 7 original open questions resolved; 5 remain, sharpest being the wake mechanism for an idle supervisor
- (decision) NO singleton lock in bagual-manager. The owner must be able to open several manager windows at once and work on different tasks in parallel. Where parallel file edits would collide, the manager creates a worktree instead of serialising behind a lock
- (decision) bagual-worktree survives into v2, scoped to ad-hoc worktree creation for parallel manager windows. Its pool mode is still superseded by bmad-loop native worktree isolation for epic runs
- (decision) bagual-tickets is essential and central: it is the persistence and control centre of the system. Work in flight, work done, and deferred work all live there so nothing is lost between sessions
- (decision) Deferred work MUST become a ticket whenever it is not resolved on the spot, so it can be picked up when convenient
- (decision) bagual-tickets is carried into v2 as-is in behaviour but fully rewritten in English - including the data files it produces (board, ticket files, JSON output)
- (decision) OPEN CONCERN: concurrent manager windows writing to the ticket board may need file locking. Must be reviewed and probably adjusted in v2
- (decision) Deferred to later, explicitly not now: gorioapp's own migration to v2, and the inherited debt (54 mis-triaged tickets, dormant Semgrep)
- (decision) Manager communication style is a cognitive-load requirement, not aesthetics: the owner reads on a phone and must decide fast. Long prose actively impairs the decision
- (event) Wake mechanism researched: bmad-loop run dir exposes ATTENTION, journal.jsonl, state.json, events/, engine.pid. Combined with harness background-bash re-invocation this gives a zero-token idle supervisor. FR14 now has a mechanism
- (decision) Three distinct persistence layers, do not conflate them: .bagual/ holds skill CONFIGURATION; the wiki holds KNOWLEDGE (code patterns, product decisions, learnings); tickets hold WORK (tasks, their intake rules, and deferred work)
- (decision) Ticket intake rules are part of the value, not overhead: check whether it was already done or implemented, whether something similar exists, whether a product decision already covers it - so the system stops undoing and redoing things
- (decision) New skill needed: bagual-wiki - creates and maintains the project wiki. Bootstraps from an existing CLAUDE.md/AGENTS.md when the project has one, plus a codebase scan, rather than starting from an empty tree
- (decision) New skill needed: bagual-rule-author - turns a green-sealed anti-pattern into a real mechanical rule and marks the ledger entry automated. This step has never had an owner, which is why five Semgrep scripts exist and zero rules do
- (decision) Semgrep rule-authoring skill is named bagual-semgrep-rules (was proposed as bagual-rule-author)
- (decision) Template skills (init/sync/push) are PARKED, not ruled out. They serve the owner's own local template projects and were deliberately left out of this round - must be revisited before v2 ships
- (decision) Self-learning records EVERY owner correction, automatically. No manual marking step - the owner should never have to flag a lesson as worth keeping
- (decision) CUT: manager does not break a large request into epics itself - that is John's job (bmad-agent-pm)
- (decision) KEEP: spec/epic work waits for the owner to be present
- (decision) KEEP (simplified): detect that a change affects the product and update the design documents
- (decision) DISABLED for now: proactive work when the queue is empty
- (decision) KEEP: warn when a ticket has been stuck too long
- (decision) KEEP: dispatch recorded in a file, never in conversation memory
- (decision) KEEP: quota tracking with automatic stop near the limit
- (decision) DISABLED BY DEFAULT, configurable: scheduled self-wake. Some projects do not need it
- (decision) CUT: morning briefing artifact
- (decision) KEEP: retrospective debt automatically becomes a ticket
- (decision) KEEP: defects in the skills themselves automatically become tickets
- (decision) CUT: self-healing (manager repairing its own skills autonomously)
- (decision) Branch/environment flow (dev, staging, promotion) is company-specific. The manager carries only generic rules; the concrete flow is asked during init. Any other highly project-specific behaviour discovered later also moves into init
- (decision) CUT: singleton session lock. Several manager windows must be able to run at once
- (decision) KEEP: crash detection and reconciliation of a session that died mid-cycle
- (decision) KEEP: orphan dispatch sweep
- (decision) KEEP: append-only diary of what was done
- (decision) KEEP: manager decides ambiguity itself instead of waking the owner
- (decision) KEEP: acts alone only with a precedent; otherwise parks the work and asks the owner
- (decision) KEEP: never repeats a decision the owner already corrected
- (decision) KEEP: owner ratifies or corrects the manager's decisions in the following session
- (decision) CUT: percentage-of-decisions-ratified metric (vanity metric)
- (decision) CUT: sampling review of historical auto-assigned track decisions - the classifier that produced them is retired
- (event) Manager feature triage complete: 24 capabilities reviewed, 14 kept, 6 cut, 2 disabled-by-default/configurable, 1 delegated to John, 1 moved into init
- (event) Research 1/3 back: BMad skill-authoring craft. Key find - bmad-workflow-builder/references/skill-quality-principles.md is the written doctrine, and bmad-workflow-builder is a meta-skill that enforces it at build time. v2 should be built through it rather than hand-rolled
- (event) Research 2/3 back: BMad infrastructure. Key find - the bmad-loop hook coordinates an out-of-process orchestrator with an in-session agent via environment-variable tagging plus atomic event-file drops, inert in normal sessions. This is the concrete model for FR14's wake mechanism
- (decision) Adopt BMad script conventions wholesale for v2: stdlib-only, PEP 723 headers, stdout=JSON contract, stderr=diagnostics, distinct exit codes (0/1/2/3), atomic temp-fsync-replace writes, append-only logs with write-only/blind ack, fail-soft on optional layers
- (event) Research 3/3 back: BMad philosophy. Headline finding - BMad measured the accumulate-documentation model, found it degrades agent performance, and DEPRECATED its own doc-generation skills. Context is framed as a liability that must keep proving itself, not an asset that accretes
- (assumption) Our wiki differs from what BMad deprecated (curated + grep-on-demand vs auto-generated + always-loaded), so it dodges the always-loaded tax. But it has no pruning test, no verified/generated trust status and no shrinking-audit rule, leaving it exposed to the staleness failure mode. Proposal: adopt all three into bagual-wiki
- (decision) ADOPTED for bagual-wiki: the pruning test - would removing this line change agent behaviour? If not, it does not go in and does not stay in
- (decision) ADOPTED for bagual-wiki: every entry carries a trust status - verified (a human confirmed it) or inferred - plus source and date. An unconfirmed claim is never laundered into fact
- (decision) ADOPTED for bagual-wiki: a curation pass must end with the knowledge base the same size or smaller, never larger. Context is a liability that keeps proving itself, not an asset that accretes
- (decision) Wiki exclusion list adopted from BMad: no code paraphrase, no file maps, no overview/tour documents, no ecosystem defaults, no history narration, no inference dressed as fact, no aspirational future state
- (event) PRD finalized. 41 functional requirements, 9 skills in the v2 roster, 3 research reports folded in. Reviewer pass deliberately not run - closed on owner instruction, flagged as unspent in open questions
- (decision) Ticket concurrency resolved: NO shared index file and NO lock. Each ticket is its own file; the board listing is derived on read by scanning them. Removes the shared write target entirely, so concurrent manager windows cannot collide and there is no lock that can go stale. Chosen over file locking (reintroduces the lock the owner just cut) and over an append-only journal (more robust but more build cost)
- (change) REVIEW FINDING (evidence, verified independently): the claim that FR14's wake mechanism is solved by the bmad-loop hook is FALSE. That hook is gated on env vars set only on sessions the orchestrator itself spawned, and is silently inert in an ordinary interactive window - exactly the case FR14 needs. Correction applied to section 10
- (change) REVIEW FINDING (adversarial, verified by git log): the destructive 22.1 migration was committed at 2026-08-03 14:23 (ad4f1fc5); the spec-gate artifact first appeared at 19:55 (de1df602), 5.5h later. The gate never had the chance to prevent the failure that motivated it. It is a reasoned bet, not a proven control. PRD reframed accordingly
- (change) REVIEW FINDING (evidence): bmad-loop worktree seeding covers adapter CLI/MCP configs only. bagual-worktree additionally replicates all gitignored deps, env files and uncommitted diffs. Retiring it wholesale was overstated - either worktree_seed is configured explicitly or hydration stays
- (change) REVIEW FINDING (adversarial): the Epic 22 write collision had two sides. FR13 hardens the loop side with worktree isolation; the manager-dispatched side is still governed by the same human judgement that already failed once
- (change) REVIEW FINDING (adversarial): knowledge growth is automatic and mandatory (FR28) while pruning is discretionary and has no trigger (FR25a). The system grows by construction
- (change) REVIEW FINDING (edge cases): a story hand-edited after PASS has no staleness trigger forcing a re-gate - reproduces 22.1's root cause one layer up
- (change) REVIEW FINDING (edge cases): verified wiki entries never expire or get re-verified, which is the exact failure FR25a's own rationale warns about
- (event) Reviewer pass complete (4 parallel lenses). 3 claims corrected in place, 5 new requirements added (FR12a, FR13a, FR25b, FR33a, FR33b), 2 open questions replaced with sharper ones. Remaining accepted-but-unapplied: glossary, FR42 numbering, assumptions round-trip, unquantified bounds in FR27
- (decision) NEW REQUIREMENT for bagual-tickets: ticket states are configurable per project. One of the owner's companies already needs a custom state for a situation that only occurs there. The config holds both the state itself AND a written explanation of when and how it is meant to be used, so the meaning travels with the state instead of living in the owner's head
- (decision) First increment confirmed: the loop-plus-closure half (FR7-FR21), most of which already exists. The three new skills and the manager/tickets rewrite come after, as a second bet
- (decision) The FR14 wake spike runs NOW, before any further documents are written - the owner does not want this verification deferred into loop execution
- (event) SPIKE RESULT - FR14 wake mechanism PROVEN by live test (2026-08-04). Two independent primitives, both fired correctly: a Monitor process watching the run directory emitted one event per ATTENTION occurrence mid-run (2 of 2) plus a terminal event, and a background shell command running the job itself re-invoked the session on exit. Zero tokens consumed while idle between events. The mechanism does NOT depend on the bmad-loop hook, which is why the earlier claim was wrong
- (event) SPIKE RESULT - end-of-run signal arrives on TWO independent paths (Monitor terminal event AND background command exit). Keep both: the redundancy costs nothing and covers the case where one path is missed
- (event) NEW REQUIREMENT from the spike: the watcher must cover every terminal state, not just the ATTENTION signal. A filter matching only the happy path stays silent through a crash, a hang or a budget kill - and silence is indistinguishable from still-running. This is a correctness requirement, not a nicety
- (decision) Delivery sequence added as section 2.0: first increment = loop + closure (FR7-FR21), second = wiki, semgrep-rules, manager/tickets rewrite and distribution. Answers the adversarial finding that a nine-skill document with no named first step is a wish, not a plan
- (decision) BOOTSTRAP ORDERING decided: Epic 1 is built BY HAND with the owner, because it delivers the very skill that would otherwise run it. From Epic 2 onward the loop-runner executes the work. Everything from Epic 2 on must therefore be authored in bmad-loop's stories format and be complete enough for literal execution
- (decision) Epic 1 scope confirmed by owner: the loop-runner skill + epic closure (deferred work, QA when configured, retrospective, knowledge capture) + spec-gate auditing the STORIES with the PASS bound to the audited version + minimal init/config + bmad-loop stories mode enabled. Rationale: without the gate upgrade and closure, Epic 2 runs unattended with no safety net
- (decision) Out of Epic 1: bagual-wiki, bagual-semgrep-rules, manager rewrite, tickets rewrite, distribution, general translation. All become Epic 2+, executed by the skill itself
- (decision) bmad-loop installed into the bagual-skills repo (2026-08-05). Three policy defaults are wrong for our case and become Epic 1 story content with their rationale: isolation none->worktree, stories source sprint-status->stories, adapter model empty->sonnet
- (decision) MANUAL STEP pending on the owner: run claude once inside bagual-skills to accept workspace trust and hook approval - loop-spawned sessions cannot answer that dialog and a pending one reads as a session timeout
- (change) Epic design found 3 PRD gaps the reviewer pass missed: bagual-semgrep-rules had NO requirements at all, wiki creation had none (only governance), and the config-merge rule was stated twice. All three closed
- (change) FR44-FR47 added for bagual-semgrep-rules: queue is a derived query; a rule must prove it fires on the real occurrence AND does not fire on accepted code before it ships; authoring marks the ledger entry automated; enforcement has a justified false-positive path that never silently accepts
- (change) FR48-FR49 added for bagual-wiki creation: bootstrap from existing project docs plus a codebase scan, subject to the pruning test FROM BIRTH (a bootstrap producing a large wiki has failed); structure exists to make grep-native retrieval converge, with no retrieval script or tag machine
- (change) FR4 no longer restates the merge algorithm - it points at FR39, which owns it
- (event) Epics and stories complete: 6 epics, 34 stories, 56 requirements, zero orphans. Final validation found 3 requirements mapped at epic level but never exercised by any story's acceptance criteria (FR7, FR34, FR31) and one genuine forward dependency in Epic 2 (bootstrap ordered before the structure it writes into). All four fixed
- (event) OPEN GAP found while routing the next step: bmad-loop stories mode consumes a spec folder (stories.yaml + SPEC.md) but NO installed skill produces stories.yaml. bmad-spec produces SPEC.md only; the Story Breakdown component that emits stories.yaml is not present in this installation. FR8 assumes stories mode. Either a producer is found/built, or Epic 2+ runs in sprint-status mode instead - this decision changes Story 1.1
- (event) Workflow complete: PRD -> epics -> stories -> validation. Artifacts live in bagual-skills/_bmad-output/planning-artifacts/
- (decision) DECISION: option (b) - drive runs from the sprint queue instead of the unproducible spec-folder mode. FR8 rewritten, Story 1.1 acceptance criteria rewritten to match
- (decision) VERIFIED before applying (b): the executor's first step compiles its context from the EPIC BREAKDOWN and its stories, then writes a derived implementation plan. It does not invent requirements, and it halts on intent gaps rather than fantasize. So FR7 survives intact - requirements are still authored upfront by the owner and audited by the gate; only the plan is derived at run time
- (decision) Story 22.1 re-read in this light and it is consistent: the executor faithfully executed an upstream document that never inventoried the old table's columns. It did not invent them. The fix was always upstream, which is what FR9 targets
- (decision) Dial kept available, not adopted: per-story-spec-approval would let the owner approve each derived plan before it executes, at the cost of the unattended run
