---
title: Notas de CRUD de veículos (fixture demo E3.3)
tipo: nota-operacional
estado: ativa
areas:
  - vehicles
---

# Notas de CRUD de veículos (fixture demo)

Documento sintético de fixture. Declara `areas:` em formato de lista em
bloco (em vez de fluxo `[a, b]`) para exercitar as duas sintaxes que o
parser minimalista de `retrieve_slice.py` precisa suportar.
