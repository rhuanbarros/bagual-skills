---
title: Ciclo reserve->attach->complete de créditos (fixture demo E3.3)
tipo: decisão-técnica
estado: ativa
areas: [credits, proposals]
---

# Ciclo reserve->attach->complete de créditos (fixture demo)

Documento sintético de fixture — declara DUAS áreas (`credits` e
`proposals`), exercitando a regra multi-área (F6): este doc deve
aparecer tanto no retrieval direto de `credits` quanto no de
`proposals`, sem precisar de expansão adjacente para nenhum dos dois.
