# <NOME-SKILL> — playbook (curado, feed-forward)

Lido no INÍCIO de todo run de `<NOME-SKILL>`. Orientação acumulada e testada na prática para rodar
esta skill bem. Curado do `lessons-log.md` pelo passo Curator: APPEND ou REFINE entradas, NUNCA
sobrescreva — deprecie uma regra obsoleta (~~riscada~~ + motivo) em vez de deletar (evita "context
collapse").

Isto é SEPARADO da memória do projeto (o Ledger/Wiki — Bloco E). Este sidecar é a memória
operacional **da própria skill**. <SE-TOPO: É alimentado adiante nos sub-agentes despachados pelo
pai (lido + injetado), não só lido uma vez.>

> Marcadores de graduação: uma regra marcada `✅ Graduada para a skill (<data>)` já é permanente no
> `references/`/`SKILL.md` da skill — não depende mais deste playbook ser lido. Sem marca =
> só-sidecar.

## Regras

<!-- Scaffold vazio — nenhuma regra ainda. As regras nascem da curadoria do lessons-log ao fim de
cada run. Formato canônico:

1. **<regra operacional específica sobre rodar esta skill>.** <detalhe> (Fonte: run <id>.)
-->
