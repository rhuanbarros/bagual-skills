# Setup — Sistema de Skills BMad + Bagual

- Create CLAUDE.md
run /init on the root

- install or update bmad
npx bmad-method install

- rename CLAUDE.md to CLAUDE_new.md

- Run this command to copy these files

SRC=/home/rhuan/2_temporario/bagual-skills
DST=/home/rhuan/2_temporario/template_fullstack_app_supabase

mkdir -p "$DST/_bmad/custom" "$DST/_bmad-output" && \
cp "$SRC/project-templates/_bmad/custom/bmad-dev-story.toml" "$DST/_bmad/custom/bmad-dev-story.toml" && \
cp "$SRC/project-templates/_bmad/custom/bmad-quick-dev.toml"  "$DST/_bmad/custom/bmad-quick-dev.toml" && \
cp "$SRC/project-templates/AGENTS.md"                         "$DST/AGENTS.md" && \
cp "$SRC/project-templates/CLAUDE.md"                         "$DST/CLAUDE.md" && \
cp "$SRC/_bmad-output/anti-patterns.md"                       "$DST/_bmad-output/anti-patterns.md" && \
cp "$SRC/_bmad-output/decisions.md"                           "$DST/_bmad-output/decisions.md" && \
cp "$SRC/_bmad-output/notes.md"                               "$DST/_bmad-output/notes.md" && \
cp "$SRC/_bmad-output/product-decisions.md"                   "$DST/_bmad-output/product-decisions.md" && \
cp "$SRC/_bmad-output/projects-history.md"                    "$DST/_bmad-output/projects-history.md"

-  Update the files using AI


```
I have several files copied from another project that need to be adapted to this one.

The source of truth for this project's actual knowledge is `CLAUDE_new.md`, generated by running /init here.

## File-by-file instructions

### AGENTS.md
Fill in all `{PLACEHOLDER}` fields using the real information from `CLAUDE_new.md` (project name, description, tech stack, how to run, current state). Preserve all formatting, sections, and BMad-specific rules exactly as they are — especially "Regras críticas", the skills table, and the BMad artifact structure block.

### CLAUDE.md
This file uses `@AGENTS.md` as an include — review it and update only references, instructions, or skill names that don't match this project's stack or tooling. If the content is still accurate, leave it unchanged.

### _bmad-output/anti-patterns.md
### _bmad-output/decisions.md
### _bmad-output/notes.md
### _bmad-output/product-decisions.md
### _bmad-output/projects-history.md
These files contain entries from the previous project — **delete all entries below the header block**. Keep the file header (the `>` block with format instructions at the top of each file) and the `---` separator. Leave the body empty. This is a new project with no history yet.

## What NOT to do
- Do not invent content for `_bmad-output` files from `CLAUDE_new.md` — those files accumulate knowledge during development, they start empty.
- Do not change the structure or wording of instructions/rules in any file unless they reference the old project name explicitly.
- Do not modify `CLAUDE_new.md`.
` structure already covers better.
6. Save the result back to `AGENTS.md`. Do not modify `CLAUDE_new.md`.

```